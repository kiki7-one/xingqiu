# kiki星球 开发计划

> **文档版本**：v1.0
> **创建日期**：2026-08-26
> **对应 PRD**：PRD.md v2.1.0
> **产品形态**：本地网页 + 本地 App（双形态，纯离线，独立文件存储）
> **MVP 范围**：P0 功能（物品 / 宠物 / 待办 / 日记 / 记账 / 语录 / 花语 / 通用功能）

---

## 一、开发总览

### 1.1 项目特点与关键约束

| 约束 | 说明 | 对开发的影响 |
|------|------|--------------|
| 纯本地运行 | 不联网、不部署、无服务端、无云数据库 | 全部逻辑在前端/客户端完成，无 API 调用 |
| 无登录 | 永久无账号体系 | 无需鉴权、无需 token、无需用户表 |
| 独立文件存储 | 单一 `kiki-data.json` 文件，刷新/关闭/重启不丢失 | App 直接读写文件；网页用 IndexedDB 镜像 + 导入导出 |
| 双形态 | 本地 App + 本地网页，功能对等、共享数据 | 一套代码 + 两套壳（Electron / 浏览器） |
| 纯离线内容 | 星座/语录/花语用预置内容库轮换 | 需准备内容数据 + 轮换算法 |
| 隐私锁 | 密码/指纹保护隐私板块 | App 用系统指纹/Face ID；网页用密码 |

### 1.2 技术选型建议

| 层 | 选型 | 理由 |
|----|------|------|
| 前端框架 | **React 18 + TypeScript** | 组件化、生态成熟、TS 类型安全 |
| UI 库 | **Tailwind CSS + 自定义组件** | 轻量、易定制温暖风格、避免重 UI 库 |
| 图表 | **ECharts**（统计/趋势图） | 功能全、离线可用 |
| 图标 | **lucide-react** | 轻量、风格统一 |
| 路由 | **React Router v6** | 标准选择 |
| 状态管理 | **Zustand** | 轻量、无 boilerplate、适合中型应用 |
| 数据存储（App） | **Electron + Node fs** 直接读写 `kiki-data.json` | App 形态主方案 |
| 数据存储（网页） | **IndexedDB**（主存储）+ 导入/导出 `.kikibak` 文件 | 浏览器无直接文件写权限，用 IndexedDB 镜像 |
| App 打包 | **Electron**（macOS + Windows） | 跨平台、可访问本地文件系统 |
| 网页构建 | **Vite** | 极速构建、原生 TS 支持 |
| 通知（App） | **Electron Notification API** → 系统通知中心 | macOS/Windows 通用 |
| 通知（网页） | **浏览器 Notification API** | 网页形态方案 |
| 加密 | **crypto-js (AES)** 加密隐私字段 | 纯前端加密 |
| 测试 | **Vitest + React Testing Library** | Vite 原生、零配置 |
| 打包发布 | Electron 应用 + 静态网页双产物 | 双形态同源 |

### 1.3 工程结构建议

```
kiki-star/
├── packages/
│   ├── core/              # 业务逻辑（与 UI 无关，双形态共享）
│   │   ├── types/         # TypeScript 类型定义（对应 PRD 实体）
│   │   ├── store/         # Zustand store（各模块状态）
│   │   ├── db/            # 数据访问层
│   │   │   ├── app-db.ts  # Electron 形态：读写 kiki-data.json
│   │   │   ├── web-db.ts  # 网页形态：IndexedDB 读写
│   │   │   └── index.ts  # 根据运行环境自动选择
│   │   ├── reminders/     # 提醒引擎（补货/疫苗/驱虫/猫砂/待办）
│   │   ├── content/       # 离线内容库（语录/花语/星座运势轮换）
│   │   ├── crypto/        # 隐私字段加密
│   │   └── utils/         # 通用工具（日期、ID 生成等）
│   ├── ui/                # React 组件库（双形态共享）
│   │   ├── pages/         # 页面组件（对应 P-01 ~ P-22）
│   │   ├── components/    # 通用组件
│   │   ├── layouts/       # 5 Tab 布局
│   │   └── styles/        # Tailwind + 主题
│   ├── electron/          # Electron 壳（App 形态）
│   │   ├── main.ts        # 主进程：文件读写、通知、指纹
│   │   ├── preload.ts     # 预加载桥
│   │   └── data/          # 默认数据文件路径
│   └── web/               # 网页形态入口
│       └── index.html     # Vite 入口
├── content-data/          # 离线内容库原始数据
│   ├── quotes/            # 语录库（JSON）
│   ├── flowers/           # 花语库（JSON + 图片）
│   └── horoscope/         # 星座运势库（12 星座 × N 天）
├── PRD.md
├── DEVELOPMENT.md         # 本文件
└── package.json
```

### 1.4 里程碑总览（MVP，预估 10-12 周）

| 里程碑 | 周期 | 产出 | 验收 |
|--------|------|------|------|
| M0 基础设施 | 第 1-2 周 | 工程骨架、数据层、双形态跑通 | 能在 App 和网页中读写同一数据文件 |
| M1 核心记录 | 第 3-5 周 | 物品、宠物、待办、记账、日记的 CRUD | 5 大记录模块可用 |
| M2 提醒引擎 | 第 6 周 | 补货、疫苗、驱虫、猫砂、待办提醒 | 提醒按规则触发本地通知 |
| M3 内容板块 | 第 7-8 周 | 每日语录、每日花语、首页聚合 | 每日内容轮换展示 |
| M4 通用功能 | 第 9 周 | 隐私锁、提醒设置、备份恢复、数据导出 | 隐私保护与备份能力完整 |
| M5 双形态整合与测试 | 第 10-11 周 | Electron 打包、网页构建、端到端测试 | 双形态可交付 |
| M6 UAT 与发布准备 | 第 12 周 | 用户验收、问题修复、文档 | MVP 可交付 |

---

## 二、分阶段详细任务

### M0：基础设施（第 1-2 周）

> **目标**：搭建工程骨架，跑通双形态读写同一数据文件。

#### 任务清单

| ID | 任务 | 说明 | 验收标准 | 预估 |
|----|------|------|----------|------|
| M0-1 | 初始化 monorepo 工程 | pnpm workspace + packages 结构 | `pnpm install` 通过，三个 package 可独立构建 | 0.5d |
| M0-2 | 配置 Vite + React + TS + Tailwind | `packages/ui` 与 `packages/web` 共用配置 | `pnpm dev` 启动网页，显示 Hello 页 | 0.5d |
| M0-3 | 配置 Electron 壳 | `packages/electron`，主进程 + preload + 加载 UI | `pnpm electron:dev` 启动 App 窗口 | 1d |
| M0-4 | 定义 TypeScript 类型 | 按 PRD 第五章实体：User/Item/Pet/Todo/Diary/Transaction 等 | 类型可被 core 与 ui 引用 | 1d |
| M0-5 | 实现 Electron 数据层 | `app-db.ts`：读写 `~/kiki-data.json`，自动初始化空结构 | App 启动读取、写入后重启不丢失 | 1d |
| M0-6 | 实现网页数据层 | `web-db.ts`：IndexedDB 镜像，提供与 app-db 相同接口 | 网页形态读写一致，刷新不丢失 | 1.5d |
| M0-7 | 数据层环境自适应 | `db/index.ts` 根据运行环境（Electron/Web）选择实现 | 切换形态自动选用对应实现 | 0.5d |
| M0-8 | 5 Tab 布局骨架 | `layouts/MainLayout.tsx`，底部导航 5 Tab + 路由占位 | 点击 Tab 切换占位页 | 1d |
| M0-9 | 主题与设计 Token | 暖色系配色、字体、间距、圆角 | 全局样式生效，视觉温暖 | 1d |
| M0-10 | 通用组件库基础 | Button/Input/Modal/Card/Empty/Tag 等 | 可在页面中引用 | 1.5d |
| M0-11 | Zustand store 骨架 | 各模块 store 占位（items/pets/todos/diary/tx/...） | store 可读写数据层 | 1d |
| M0-12 | 跨形态数据一致性验证 | 在 App 写入，网页导入；反向亦然 | 数据文件格式统一，可互相恢复 | 1d |

**M0 交付物**：可运行的 App + 网页双形态骨架，5 Tab 切换，读写同一数据文件。

---

### M1：核心记录模块（第 3-5 周）

> **目标**：完成 5 大记录板块的完整 CRUD 与列表展示。

#### M1-A 物品板块（第 3 周，5 天）

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M1-A1 | 物品列表页（P-03） | F-1.1 | 列表展示、分类筛选、名称搜索、补货角标 | 1d |
| M1-A2 | 物品新增/编辑表单 | F-1.1 | 字段校验、图片选择、分类/单位选择 | 1d |
| M1-A3 | 物品详情页（P-04） | F-1.1 | 展示字段、编辑/删除、快速加减库存 | 1d |
| M1-A4 | 消耗记录 | F-1.2 | 消耗/补充记录、库存联动、历史列表 | 1d |
| M1-A5 | 购物清单页（P-05） | F-1.4 | 手动添加、勾选已购回写库存、删除 | 0.5d |
| M1-A6 | 电商跳转 | F-1.5 | 联网时跳转淘宝/京东/拼多多，离线置灰 | 0.5d |

#### M1-B 宠物板块（第 3-4 周，6 天）

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M1-B1 | 宠物档案页（P-06） | F-2.1 | 多宠物管理、头像横向切换、增删改 | 1.5d |
| M1-B2 | 疫苗提醒 | F-2.2 | 记录接种、下次日期推算、提醒开关 | 1d |
| M1-B3 | 驱虫提醒 | F-2.3 | 体内/体外、自定义周期、记录历史 | 0.5d |
| M1-B4 | 健康提醒（自定义） | F-2.4 | 自定义事项（体检/洗澡/美容/剪指甲）、周期 | 1d |
| M1-B5 | 猫粮/狗粮余量 | F-2.5 | 录入重量+消耗、模糊等级展示、不足自动加购物清单 | 1d |
| M1-B6 | 猫砂提醒 | F-2.6 | 更换周期记录、到期提醒 | 0.5d |
| M1-B7 | 成长记录（P1，本期可简版） | F-2.7 | 体重记录 + 大事记（照片时间轴可后置） | 0.5d |

#### M1-C 记账板块（第 4 周，4 天）

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M1-C1 | 收支记录页（P-07） | F-8.1 | 新增/编辑/删除、分类管理、日期选择 | 1.5d |
| M1-C2 | 预算与统计页（P-08） | F-8.2 | 月度/分类预算、饼图柱图折线图、超支提醒 | 1.5d |
| M1-C3 | 账单导出 | F-8.3 | 导出 Excel/图片、时间范围选择 | 1d |

#### M1-D 日记板块（第 5 周，3 天）

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M1-D1 | 日记列表页（P-10） | F-5.2 | 时间轴倒序、关键词/标签/日期搜索 | 1d |
| M1-D2 | 日记编辑页（P-11） | F-5.1 | 文字/图片/心情/天气/标签、保存校验 | 1d |
| M1-D3 | 日记导出 | F-5.3 | 导出 PDF/长图、单篇/月/全部 | 1d |

#### M1-E 待办板块（第 5 周，2 天）

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M1-E1 | 待办清单页（P-12） | F-3.1 | 新增/编辑/删除/完成、优先级、重复周期 | 1.5d |
| M1-E2 | 重复任务生成引擎 | F-3.1 | 每日/每周/每月/自定义自动生成 | 0.5d |

**M1 交付物**：5 大记录模块完整可用，数据持久化到本地文件。

---

### M2：提醒引擎（第 6 周）

> **目标**：所有提醒按规则触发本地通知，支持免打扰。

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M2-1 | 提醒引擎核心 | F-1.3,F-2.2~2.6,F-3.1 | 定时扫描数据，匹配触发条件 | 1d |
| M2-2 | 本地通知（App） | F-1.3 | Electron Notification 触发系统通知 | 0.5d |
| M2-3 | 本地通知（网页） | F-1.3 | 浏览器 Notification API | 0.5d |
| M2-4 | 补货提醒规则 | F-1.3 | 库存≤阈值触发、24h 去重、忽略后重提 | 0.5d |
| M2-5 | 宠物提醒规则 | F-2.2~2.6 | 疫苗提前 7/3/1 天、驱虫/猫砂/健康周期 | 1d |
| M2-6 | 待办到期提醒 | F-3.1 | 截止时间到触发、支持延迟 15min/1h/明天 | 0.5d |
| M2-7 | 粮食余量推算引擎 | F-2.5 | 每日扣减、等级判定、不足自动加购物清单 | 0.5d |
| M2-8 | 免打扰时段 | F-22.3 | 设定时段内不推送，延迟到时段结束后汇总 | 0.5d |

**M2 交付物**：提醒引擎可用，各类提醒按规则触发本地通知。

---

### M3：内容板块与首页（第 7-8 周）

> **目标**：完成每日语录、每日花语、首页聚合展示。

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M3-1 | 离线语录库 | F-9.1 | 整理 365+ 条语录 JSON（励志/治愈/爱情/友情分类） | 1d |
| M3-2 | 离线花语库 | F-10.1 | 整理 365+ 种花 JSON（花名/花语/图片/养护）+ 花图片资源 | 1.5d |
| M3-3 | 每日语录页（P-16） | F-9.1 | 按日期轮换展示、换一条（每日限 3 次） | 0.5d |
| M3-4 | 语录卡片生成 | F-9.1 | Canvas 生成精美卡片图片、保存本地 | 1d |
| M3-5 | 语录收藏（P-16） | F-9.2 | 收藏/取消、收藏列表 | 0.5d |
| M3-6 | 每日花语页（P-17） | F-10.1 | 每日一花、往期历史、收藏 | 1d |
| M3-7 | 首页今日概览（P-02） | F-3.2 | 聚合今日待办/提醒/语录/花语/星座摘要 | 2d |
| M3-8 | 首页快速操作 | F-3.2 | 首页直接勾选完成待办 | 0.5d |
| M3-9 | 内容库导入扩展 | F-9.1 | 用户可导入自定义语录/花语包 | 0.5d |

> **注**：星座板块（F-6.1/6.2）为 P1，本期只做首页摘要占位，V1.1 再完整实现。

**M3 交付物**：首页一屏展示今日要点，每日内容轮换，语录卡片可生成保存。

---

### M4：通用功能（第 9 周）

> **目标**：完成隐私、设置、备份、导出、个人资料。

| ID | 任务 | 对应 PRD | 验收标准 | 预估 |
|----|------|----------|----------|------|
| M4-1 | 个人资料页（P-19） | F-22.5 | 昵称/头像/性别/生日编辑保存 | 0.5d |
| M4-2 | 隐私锁（P-21） | F-22.2 | 密码设置、App 指纹/Face ID、网页密码、加密隐私字段 | 1.5d |
| M4-3 | 隐私板块拦截 | F-22.2 | 进入日记/记账/宠物前弹锁，解锁后访问 | 0.5d |
| M4-4 | 提醒设置页（P-20） | F-22.3 | 全局提醒开关、各类独立开关、免打扰时段 | 1d |
| M4-5 | 数据导出页（P-22） | F-22.4 | 导出 Excel/PDF/JSON、按模块选择 | 1d |
| M4-6 | 数据备份 | F-22.4.1 | 导出 `.kikibak`、选择保存路径 | 0.5d |
| M4-7 | 数据恢复 | F-22.4.1 | 选择 `.kikibak`、二次确认、恢复前自动备份 | 1d |
| M4-8 | 自动备份 | F-22.4.1 | 定时（默认每周）生成到 `~/kiki-backups/` | 0.5d |
| M4-9 | 备份历史 | F-22.4.1 | 列表展示、恢复/删除 | 0.5d |

**M4 交付物**：隐私保护、备份恢复、数据导出完整可用。

---

### M5：双形态整合与测试（第 10-11 周）

> **目标**：Electron 打包、网页构建、端到端测试。

| ID | 任务 | 说明 | 验收标准 | 预估 |
|----|------|------|----------|------|
| M5-1 | Electron 打包（macOS） | `electron-builder` 生成 `.dmg` | 可在 macOS 安装运行 | 1d |
| M5-2 | Electron 打包（Windows） | 生成 `.exe` 安装包 | 可在 Windows 安装运行 | 1d |
| M5-3 | 网页构建产物 | Vite 构建静态文件 | 本地浏览器打开可用 | 0.5d |
| M5-4 | 双形态数据共享验证 | App 写入→网页导入；反向亦然 | 数据一致 | 1d |
| M5-5 | 单元测试（core 层） | Vitest 覆盖 db/reminders/content/crypto | 核心逻辑测试通过 | 1.5d |
| M5-6 | 组件测试（ui 层） | React Testing Library 覆盖关键页面 | 关键交互测试通过 | 1d |
| M5-7 | 端到端测试 | Playwright 跑通主流程（记录→提醒→备份） | E2E 通过 | 1.5d |
| M5-8 | 性能优化 | 首屏 ≤ 2s、列表虚拟化、数据文件读写优化 | 性能达标 | 1d |
| M5-9 | 视觉走查 | 对照设计稿/暖色主题走查 | 视觉一致 | 0.5d |
| M5-10 | 异常处理补全 | 空数据、文件损坏、磁盘不足等异常提示 | 异常路径覆盖 | 1d |

**M5 交付物**：可交付的 App 安装包 + 网页构建产物 + 测试通过。

---

### M6：UAT 与发布准备（第 12 周）

| ID | 任务 | 说明 | 预估 |
|----|------|------|------|
| M6-1 | 用户验收测试 | 用户实际使用，记录问题 | 2d |
| M6-2 | 问题修复 | 修复 UAT 发现的 P0/P1 问题 | 2d |
| M6-3 | 用户文档 | 简易使用说明（备份恢复、隐私锁等） | 0.5d |
| M6-4 | 发布 | 产出 App 安装包 + 网页文件 + 文档 | 0.5d |

**M6 交付物**：MVP 正式可交付。

---

## 三、关键实现要点

### 3.1 数据文件格式（建议 JSON）

```json
{
  "version": "2.1.0",
  "createdAt": "2026-08-26T10:00:00Z",
  "updatedAt": "2026-08-26T10:00:00Z",
  "profile": { "nickname": "", "avatar": "", "gender": "", "birthday": "" },
  "settings": {
    "reminders": { "globalEnabled": true, "quietHours": { "start": "22:00", "end": "08:00" } },
    "autoBackup": { "enabled": true, "frequency": "weekly" },
    "privacy": { "enabled": false, "passwordHash": "" }
  },
  "items": [],
  "consumptionRecords": [],
  "shoppingList": [],
  "pets": [],
  "petReminders": [],
  "todos": [],
  "diaries": [],
  "transactions": [],
  "budgets": [],
  "quoteFavorites": [],
  "flowerFavorites": [],
  "contentOverrides": { "quotes": [], "flowers": [] }
}
```

### 3.2 双形态数据共享方案

- **App（Electron）**：主进程通过 `fs` 直接读写 `~/kiki-data.json`，UI 通过 preload 桥调用。
- **网页**：用 IndexedDB 作为运行时存储；提供"从文件导入"和"导出到文件"功能，与 App 的 `.kikibak` 互通。
- **统一接口**：`db.read()` / `db.write(data)` / `db.export()` / `db.import(file)` 两个实现都遵循同一接口。

### 3.3 离线内容库轮换算法

```ts
// 语录：按日期取模轮换
function getQuoteOfDay(date: Date, quotes: Quote[]): Quote {
  const dayOfYear = getDayOfYear(date);
  return quotes[dayOfYear % quotes.length];
}
```

### 3.4 提醒引擎调度

- App 形态：Electron 后台定时任务（`setInterval` 每 5 分钟扫描一次）。
- 网页形态：页面打开时扫描 + `setInterval`；后台时由浏览器 Service Worker（可选）。
- 去重：每条提醒记录 `lastTriggeredAt`，24h 内不重复。

### 3.5 隐私锁加密

- 密码用 PBKDF2 哈希存储。
- 日记内容、记账备注等隐私字段用 AES 加密后存入数据文件。
- App 形态可调用系统指纹/Face ID（`TouchID.prompt()` / Windows Hello）。

---

## 四、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 网页形态无法直接写本地文件 | 网页形态数据持久化受限 | 用 IndexedDB；提供导入/导出文件与 App 互通 |
| Electron 打包体积较大 | App 安装包 100MB+ | 可接受；后续考虑 Tauri 替代 |
| 离线内容库初始内容量大 | 准备 365 条语录 + 365 种花耗时 | 可首版准备 90 条，后续扩充；支持用户导入 |
| 双形态数据格式漂移 | App 与网页数据不一致 | 严格版本号字段，统一接口，E2E 验证 |
| 浏览器 Notification 权限 | 用户未授权则网页无提醒 | 首次进入引导授权，未授权时降级为站内提示 |
| 数据文件损坏 | 用户数据丢失 | 每次写入前自动备份上一版；恢复功能 |

---

## 五、版本节奏

| 版本 | 范围 | 预估周期 |
|------|------|----------|
| **v1.0 (MVP)** | P0：物品/宠物/待办/日记/记账/语录/花语/通用 | 10-12 周 |
| v1.1 | P1：学习/星座/饮食/习惯打卡/纪念日/数据周报/历史消耗统计/宠物成长记录 | 6-8 周 |
| v1.2+ | P2：情绪记录/植物养护/家务/阅读清单/衣橱/优惠/宠物百科/AI 建议 | 按需 |

---

## 六、交付物清单（MVP）

1. **Electron App 安装包**（macOS `.dmg` + Windows `.exe`）
2. **网页构建产物**（静态文件，本地浏览器打开可用）
3. **统一数据文件格式**（`kiki-data.json` + `.kikibak` 备份）
4. **离线内容库**（365 条语录 + 90-365 种花语）
5. **测试报告**（单元测试 + E2E 测试通过）
6. **用户使用说明**（备份恢复、隐私锁、双形态数据共享）

---

> **文档结束**
> 本开发计划基于 PRD v2.1.0，MVP 范围聚焦 P0 功能。开发过程中如 PRD 变更，需同步更新本计划。
